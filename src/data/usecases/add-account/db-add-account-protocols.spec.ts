import * as DbAddAccountProtocols from "./db-add-account-protocols"

describe("DbAddAccountProtocols", () => {
  test("Should have exports", () => {
    expect(DbAddAccountProtocols).toEqual(expect.any(Object))
  })
})
